<%= form_for(@app_supplier) do |f| %>
  <% if @app_supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_supplier.errors.count, "error") %> prohibited this app_supplier from being saved:</h2>

      <ul>
      <% @app_supplier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_name %><br />
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :full_name %><br />
    <%= f.text_field :full_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
