<%= form_for(@app_action) do |f| %>
  <% if @app_action.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_action.errors.count, "error") %> prohibited this app_action from being saved:</h2>

      <ul>
      <% @app_action.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :app_module_id %><br />
    <%= f.text_field :app_module_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
