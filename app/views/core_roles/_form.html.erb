<%= form_for(@core_role) do |f| %>
  <% if @core_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@core_role.errors.count, "error") %> prohibited this core_role from being saved:</h2>

      <ul>
      <% @core_role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
