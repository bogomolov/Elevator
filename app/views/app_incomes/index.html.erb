<h1>Listing app_incomes</h1>

<table>
  <tr>
    <th>Batch</th>
    <th>Car num</th>
    <th>Weight brutto</th>
    <th>Moisture</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @app_incomes.each do |app_income| %>
  <tr>
    <td><%= app_income.batch_id %></td>
    <td><%= app_income.car_num %></td>
    <td><%= app_income.weight_brutto %></td>
    <td><%= app_income.moisture %></td>
    <td><%= link_to 'Show', app_income %></td>
    <td><%= link_to 'Edit', edit_app_income_path(app_income) %></td>
    <td><%= link_to 'Destroy', app_income, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New App income', new_app_income_path %>
